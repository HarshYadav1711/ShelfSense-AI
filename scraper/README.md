# Scraper Module

This folder is reserved for Selenium-based ingestion scripts.

- Keep browser automation scripts under `scraper/selenium_jobs/`.
- Keep extracted raw files under `samples/raw/`.
- Push normalized payloads to the backend ingestion API (`/api/v1/ingestion/`).
