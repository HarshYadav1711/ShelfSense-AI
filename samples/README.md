# Sample Data

Submission-ready sample assets for quick reviewer validation.

- `raw/`: source-like scraper payload snapshots
- `processed/`: normalized metadata examples
- `api/`: sample requests and representative API responses

Generate local database-backed sample records with:

```bash
cd backend
python manage.py generate_sample_books
```
